@model IEnumerable<ContraloriaNDSWeb.Models.Entes>

@{
    ViewBag.Title = "Entes Descentralizados";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Entes Descentralizados</h2>

<p>
    @Html.ActionLink("Nuevo Ente", "Create", new { }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.EntesId }, new { @class = "btn btn-warning" }) |
            @Html.ActionLink("Detalle", "Details", new { id = item.EntesId }, new { @class = "btn btn-info" }) |
            @Html.ActionLink("Borrar", "Delete", new { id = item.EntesId }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
